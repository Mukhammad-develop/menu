# MySQL connection string (cPanel → MySQL Databases).
# Leave empty to run in demo mode with built-in data.
DATABASE_URL="mysql://USER:PASSWORD@localhost:3306/DBNAME"

# Password for /admin (defaults to "admin1234" when unset).
ADMIN_PASSWORD=
